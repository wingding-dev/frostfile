__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/
*.import-tmp

# Credentials never belong here — this repo is public. Real secrets live in
# ~/.config/identilock-dev/ (mode 600). These patterns are a backstop against
# a stray copy, not the storage mechanism.
*.pem
*.p8
*.p12
*.pfx
*-sftp
*credentials*
*secret*
.env
.env.*
