"""Identilock — a local-only identity-control tracker.

Nothing in this package sends data anywhere. The only outbound network calls
are to Have I Been Pwned, and only when the user explicitly triggers a check.
"""

__version__ = "0.2.0"
